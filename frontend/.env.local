# Server-side proxy (Next.js rewrites) - always use localhost for local dev
API_URL=http://localhost:3001

# Client-side public URL - use network IP so mobile/other devices can also connect
NEXT_PUBLIC_API_URL=http://192.168.31.63:3001
